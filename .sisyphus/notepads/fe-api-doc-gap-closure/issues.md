# Issues

## 2026-01-27 Task: task-1 verification
- `lsp_diagnostics` project-level failed: no LSP server configured for empty extension in this environment.
- `bun run lint` fails with existing errors in unrelated files (e.g., unused vars in `src/components/antrean/antrean-header.tsx`, explicit-any in `src/components/layout/sidebar.tsx`, react-refresh warnings in `src/components/ui/*`, purity error in `src/components/ui/card-skeleton.tsx`). These pre-exist the reservation endpoint change.

## 2026-01-28 Task: task-2 verification
- `lsp_diagnostics` project-level failed again: no LSP server configured for extension when targeting `src/`.
- `bun run lint` fails with pre-existing lint errors in unrelated files (same set as prior run).
- `bun run build` fails with pre-existing TypeScript errors in unrelated files (e.g., `src/components/antrean/antrean-header.tsx`, `src/components/component-example.tsx`, `src/components/ui/combobox.tsx`, `src/routes/administrasi/antrean.tsx`).

## 2026-01-28 Task: task-3 verification
- `lsp_diagnostics` shows a deprecation hint in `src/routes/administrasi/antrean.tsx` (Zod string params). No new errors in modified logic.
- `bun run lint` still fails with pre-existing errors in unrelated files; warnings include `react-hooks/exhaustive-deps` in `src/routes/administrasi/antrean.tsx` (existing pattern).
- `bun run build` still fails with pre-existing TypeScript errors in unrelated files (same as previous run).

## 2026-01-28 Task: task-4 verification
- `lsp_diagnostics` on `src/routes/laporan/index.tsx` is clean.
- `bun run lint` failed with pre-existing errors + a new error in `src/routes/laporan/index.tsx` (explicit-any). Needs fix.
- `bun run build` failed with pre-existing TypeScript errors in unrelated files (e.g., combobox export, component-example, antrean-header). Unrelated to reports changes.

## 2026-01-28 Task: task-4 verification (post-fix)
- `lsp_diagnostics` on `src/routes/laporan/index.tsx` is clean.
- `bun run lint` still fails with pre-existing errors only; no laporan-specific errors now.
- `bun run build` still fails with pre-existing TypeScript errors in unrelated files.

## 2026-01-28 Task: 5-11 (Pengaturan CRUD pages) - COMPLETED
- All 6 CRUD pages created in `src/routes/pengaturan/`:
  - `template-pesan.tsx` (Message Template)
  - `konfigurasi-pengingat.tsx` (Reminder Config)
  - `konfigurasi-whatsapp.tsx` (WhatsApp Config)
  - `konfigurasi-sistem.tsx` (Config/Sistem)
  - `faq.tsx` (FAQ)
  - `jadwal-kontrol.tsx` (Checkup Schedule)
- **FIXED**: All type errors resolved:
  1. `template-pesan.tsx`: Fixed field names (`template_name`/`message`), PaginationControls props, ConfirmDialog props
  2. `konfigurasi-pengingat.tsx`: Removed unused imports, fixed ConfirmDialog prop
  3. `jadwal-kontrol.tsx`: Fixed hook usage, data access patterns, property names
- Route path errors remain but are expected (will resolve when TanStack Router generates route tree)
- Build now passes except for pre-existing errors in other files

## 2026-01-28 Verification
- `bun run lint` fails due to pre-existing issues in unrelated files (`antrean-header.tsx`, `component-example.tsx`, `combobox.tsx`, `master/users.tsx`, etc.).
- New lint error in `src/routes/pengaturan/faq.tsx`: react-hooks/refs ("Cannot access refs during render") on the `<form onSubmit={handleSubmit(onSubmit)}>` line. Needs follow-up to satisfy lint rule.
- Route path errors (`/pengaturan/*`) remain expected until `routeTree.gen.ts` is regenerated by TanStack Router.
